"""Continual-learning research package.

Public exports are kept minimal so that importing the package never
forces an import of heavy optional dependencies (torch, chromadb).
Sub-packages are imported on demand.
"""

__version__ = "0.0.1"
